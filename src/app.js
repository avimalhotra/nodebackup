//console.log(process.version);

let ext=require('./ext');                   // ext module
const os=require("os");                     // build in 
const colors=require('colors');             // 3rd part from npmjs.org

//console.log(id, pass);

//console.log("RED".red);
//console.log("GREEN".green);

//console.log(ext);
//console.log(ext.x, ext.y);